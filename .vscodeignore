.git/**
.gitignore
.vscodeignore
.DS_Store
**/.DS_Store
*.vsix

# The README points at the screenshot on GitHub raw, so it does not
# need to ship inside the extension package. The icon is the exception:
# it must be inside the .vsix for the Marketplace to pick it up.
images/**
!images/icon.png
